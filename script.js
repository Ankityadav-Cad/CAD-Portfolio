const fallback=[{id:1,title:'A1AN WINKER DRIVER TESTING PANEL + FIXTURE',desc:'Special Purpose Machine project involving mechanical design and fixture development for testing applications.',tags:'SPM DESIGN · CAD · FIXTURE',image:'',model:''},{id:2,title:'CONNECTING ROD',desc:'Mechanical component modelling project developed in AutoCAD with focus on engineering geometry and drawing practice.',tags:'AUTOCAD · COMPONENT',image:'',model:''},{id:3,title:'CONSTANT MESH GEARBOX',desc:'3D gearbox modelling and assembly study in Fusion 360, including mechanical components and animation.',tags:'FUSION 360 · ASSEMBLY',image:'',model:''},{id:4,title:'BMW M8 — SURFACE MODEL',desc:'Automotive surface-modelling exercise in Fusion 360 using Patch Mode to study complex curves and form development.',tags:'FUSION 360 · SURFACE',image:'',model:''}];
const key='ankitPortfolioProjects';let projects=JSON.parse(localStorage.getItem(key)||'null')||fallback;
function render(){document.querySelector('#projectGrid').innerHTML=projects.map((p,i)=>`<article class="project ${i===0?'featured':''}"><div class="visual">${p.image?`<img src="${p.image}" alt="${p.title}">`:`<div class="placeholder"><b>PROJECT IMAGE</b><span>CAD render will appear here after publishing.</span></div>`}<span class="tag">${p.tags.split(' · ')[0]||'PROJECT'}</span><span class="drawing">PROJECT_${String(p.id).padStart(3,'0')}</span></div><div class="info"><p>PROJECT_${String(p.id).padStart(3,'0')}</p><h3>${p.title}</h3><em></em><p class="desc">${p.desc}</p><div class="meta">${p.tags}</div>${p.model?`<a class="btn primary" style="display:inline-block;margin-top:20px" href="${p.model}" target="_blank">OPEN 3D MODEL ↗</a>`:''}</div></article>`).join('')}
render();